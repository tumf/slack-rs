## 1. 挙動変更の実装

- [ ] 1.1 `auth::export_profiles` で `--all` 実行時にトークン欠損プロファイルを収集してスキップする（`src/auth/export_import.rs` を確認し、欠損時に即時エラーにならないこと）
- [ ] 1.2 スキップ対象が0件でない場合は警告情報を返せるようにし、0件なら明示的エラーで終了する（`src/auth/export_import.rs` の分岐を確認）

## 2. CLI 出力と i18n

- [ ] 2.1 `auth export --all` の警告/サマリ出力を CLI で表示する（`src/cli/handlers.rs` の `handle_export_command` を確認）
- [ ] 2.2 追加メッセージを `src/auth/i18n.rs` に定義し、日本語/英語の両方で整合する（メッセージキーと文言を確認）

## 3. テストと検証

- [ ] 3.1 `--all` で一部欠損があっても成功するテストを追加する（`src/auth/export_import.rs` のテストに追加し、ケースが網羅されていることを確認）
- [ ] 3.2 `--all` で全件欠損時にエラーになるテストを追加する（エラー種別/メッセージが想定通りであることを確認）
- [ ] 3.3 `cargo test` を実行して関連テストが通ることを確認する（コマンド出力で失敗がないこと）
