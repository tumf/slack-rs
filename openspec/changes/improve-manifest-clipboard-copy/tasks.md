- [ ] クリップボードコピー用の共通関数を追加し、OSC52/arboard/OS コマンドの順序で試行する（完了確認: 新規モジュールまたは関数が追加され、呼び出し元に統合されていることを該当ファイルで確認）
- [ ] SSH 環境判定（TTY + SSH 環境変数）と TMUX passthrough 対応の OSC52 文字列生成を実装する（完了確認: 生成関数の単体テストで期待されるエスケープ列を検証）
- [ ] `generate_and_save_manifest()` から新しいコピー関数を呼び出すように配線する（完了確認: `src/auth/commands.rs` のコピー処理が置き換わっていることを確認）
- [ ] 失敗時は短い警告、`SLACK_RS_DEBUG=1` 時は詳細ログを出すようにする（完了確認: ログ出力箇所のコードレビューで条件分岐を確認）
- [ ] フォールバックに使用する OS コマンドの存在確認と実行処理を実装する（完了確認: 実行前に存在確認があることをコードで確認）
- [ ] クリップボード関連のユニットテストを追加する（完了確認: `cargo test` で追加テストが通ることを確認）
