- [ ] `conv search` コマンドの引数解析と実行パスを追加する（確認: `slack-rs conv search <pattern>` が実行できる）
- [ ] `conv search` に `name:<pattern>` フィルタを注入して `conv list` と同じ出力形式を返す（確認: 既存のフィルタ適用ロジックで結果が絞り込まれる）
- [ ] `conv search --select` を実装し、選択されたチャンネル ID のみを出力する（確認: 対話選択の結果が標準出力に出る）
- [ ] `conv list --help` に `--filter`/`--format`/`--sort`/`--sort-dir` の説明を追加する（確認: ヘルプ出力に各項目が含まれる）
- [ ] 追加機能のテストを追加する（確認: フィルタ適用と選択ロジックのユニットテストが通る）
