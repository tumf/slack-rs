- [ ] `conv list` の `--filter` パーサを実装し、`name:<glob>`, `is_member:true|false`, `is_private:true|false` の解釈が仕様通りであることをユニットテストで確認する。
- [ ] 取得した会話一覧にフィルタを適用し、AND 条件で絞り込まれることをテストで確認する。
- [ ] `conv select` コマンドを追加し、会話一覧からの選択結果としてチャンネル ID を返すことをテストで確認する（プロンプトはスタブで検証）。
- [ ] `conv history --interactive` を実装し、選択したチャンネル ID が履歴取得に使われることをテストで確認する。
- [ ] インタラクティブ UI の抽象化を追加し、非対話テストで再現できることを確認する。
- [ ] 会話一覧の取得は HTTP モックで検証し、外部 Slack API 依存なしでテストできることを確認する。
