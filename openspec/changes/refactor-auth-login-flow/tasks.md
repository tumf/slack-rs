# タスク

- [ ] `auth login` の引数と設定を集約する構造体（例: `LoginConfig`）を追加する（確認: `login_with_credentials` の引数が簡素化されている）
- [ ] 設定解決/入力解決/フロー開始を個別ヘルパー関数に分割する（確認: `src/auth/commands.rs` の関数が短くなり責務が分離されている）
- [ ] 既存のエラーハンドリングとメッセージが維持されていることを確認する（確認: 主要な分岐で同じエラーパスが使われている）
- [ ] 回帰確認としてテストを実行する（確認: `cargo test auth` または `cargo test`）
