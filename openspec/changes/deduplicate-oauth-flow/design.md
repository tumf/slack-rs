# 設計メモ: OAuth フロー一本化

## 方針
- 認証 URL 生成、ブラウザ起動、コールバック待受、コード交換、レスポンス抽出を単一コア関数へ集約する。
- 既存の標準ログイン/拡張ログインはコア呼び出しの前後処理のみ担当する。

## 期待効果
- OAuth 仕様変更時に修正箇所を 1 箇所へ限定できる。
- 回帰テストの焦点を明確化できる。

## 検証
- 既存統合テストを維持し、重複削減後の経路差分がないことを確認する。
- 実ネットワーク依存を避けるため、可能な範囲で mock/stub ベースの検証を追加する。
