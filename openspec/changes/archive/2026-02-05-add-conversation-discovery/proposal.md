# 提案: 会話探索のフィルタリングとインタラクティブ選択

## 概要
`conv list` にフィルタリングオプションを追加し、チャンネル選択を支援する `conv select` と `conv history --interactive` を提供する。

## 背景
チャンネル ID の特定が手間であり、複数コマンドや外部ツールが必要になっている。

## 目的
- チャンネル名・参加状態・公開/非公開での絞り込み
- 対話的なチャンネル選択の提供

## 非目的
- 取得対象の会話一覧自体の API 仕様変更
- GUI の提供
