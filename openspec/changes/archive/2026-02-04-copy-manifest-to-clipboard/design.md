# 設計: クリップボードコピー

## 方針
- クロスプラットフォーム対応の Rust クリップボードクレートを採用し、マニフェスト生成後に文字列をコピーする
- クリップボード操作は補助機能と位置付け、失敗しても処理を中断しない

## 代替案
- OS 別のコマンド呼び出し（macOS: `pbcopy` など）
  - 依存分岐と失敗パターンが増え、移植性が低い

## 決定
- `arboard` を使用し、`Clipboard::set_text` の失敗は警告として扱う
