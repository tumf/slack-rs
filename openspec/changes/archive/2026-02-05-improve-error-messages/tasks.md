- [x] 既知エラーコードのガイダンスマッピング（原因/解決策）を実装する（確認: `not_allowed_token_type` / `missing_scope` / `invalid_auth` がマッピングされている）
- [x] `api call` とラッパーコマンドの出力前にガイダンスを標準エラーへ表示する（確認: 既知エラー時に stderr へ文言が出る）
- [x] ガイダンス出力のユニットテストを追加する（確認: フィクスチャ JSON で stderr 文言を検証）
- [x] 既存テストを実行して回帰がないことを確認する（確認: `cargo test` が成功する）
