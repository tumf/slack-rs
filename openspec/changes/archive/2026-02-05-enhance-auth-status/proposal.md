# 提案: `auth status` の情報拡張

## 概要
`slack-rs auth status` の出力に、利用可能なトークン種別（User/Bot）、各スコープ、Bot ID、既定トークン種別を表示する。

## 背景
権限不足や API エラーの原因調査時に、現在どのトークン種別が使えるのか・どのスコープがあるのかが分からずデバッグが難しい。

## 目的
- トークン種別とスコープの可視化
- 既定トークン種別の明示
- Bot Token の Bot ID を明示

## 非目的
- OAuth フローの変更
- `auth status` 以外の出力フォーマット変更
