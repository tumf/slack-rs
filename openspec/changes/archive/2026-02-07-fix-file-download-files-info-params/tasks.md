- [x] `file download` の `files.info` 呼び出しで `file_id` を JSON ボディではなくフォーム/クエリで送るよう実装を修正する（確認: `invalid_arguments` の再現ケースが解消される）
- [x] `files.info` リクエストのモック検証を追加し、`file_id` がフォーム/クエリに含まれることを確認する（確認: モックが期待するパラメータでマッチする）
- [x] JSON ボディ送信を検知するスタブ検証を追加し、`file_id` を JSON として送った場合にテストが失敗することを確認する（確認: 再発防止のガードとして機能する）
- [x] `file download` の成功系テストを更新/追加し、`files.info` 成功後に既存のダウンロードフローが維持されることを確認する
