# 設計メモ: エラー終了共通化

## 方針
- 同一責務（失敗メッセージ出力 + 終了）を単一ヘルパーへ集約する。
- コマンドごとの文言接頭辞は保持し、終了コード規約のみ共通化する。

## 想定構成
- `main` 内に小さなヘルパーを追加し、以下を引数で受ける:
  - 表示メッセージの接頭辞
  - エラー本体
  - 終了コード
- 非対話エラー判定が必要なコマンドは既存判定関数を利用して 1/2 を選択する。

## 互換性
- 出力先（stderr）・文言フォーマット・終了コードは既存互換とする。
