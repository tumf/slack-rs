# Review Gauntlet Checkpoint

- Checkpoint state: complete
- Usable as review base: True
- Review base commit: 423a908c91732aa3683db17af9400b63af111216
- Session ID: RGS-d7d1a8919a6e
- Created at: 2026-06-22T10:01:46.016696Z (generation timestamp)

## Coverage

| State | Count |
| --- | ---: |
| reviewed | 151 |

## Findings

| ID | State | Path | Rule | Content |
| --- | --- | --- | --- | --- |
| RGF-0001 | confirmed | .codecov.yml | ci-reproducibility | Codecov の status check をすべて off にしているため、カバレッジ低下が CI 上で検出されず再現可能な品質ゲートとして機能しません。最低限 patch か project のしきい値を明示し、意図しない低下だけを失敗させる設定にしてください。 |
| RGF-0002 | confirmed | .github/workflows/release.yml | ci-reproducibility | Release builds are not reproducible because mutable runner labels are used. `ubuntu-latest`, `macos-latest`, and `windows-latest` can move to newer OS images and toolchains without any workflow change, changing produced artifacts or breaking releases. Pin explicit runner versions and update them deliberately. |
| RGF-0003 | confirmed | .github/workflows/release.yml | ci-reproducibility | The Rust toolchain is installed from the mutable `stable` channel, so the compiler version used for a given tag changes over time. Release artifacts should be built with a pinned Rust version, preferably from `rust-toolchain.toml` or an explicit version, to make rebuilds reproducible. |
| RGF-0004 | confirmed | .github/workflows/ci.yml | ci-reproducibility | The lint job downloads the moving `latest` prek installer directly from GitHub, so identical workflow revisions can install different prek versions over time and produce non-reproducible lint results. Pin a specific prek release and update the cache key to the same version. |
| RGF-0005 | confirmed | .github/workflows/ci.yml | ci-reproducibility | The Rust toolchain is selected with the floating `stable` channel, so builds and tests can change behavior when stable advances. Pin a concrete toolchain version here, or read it from a committed rust-toolchain.toml file, to make CI runs reproducible. |
| RGF-0006 | confirmed | .pre-commit-config.yaml | ci-reproducibility | The remote pre-commit-hooks repository is pinned only by a mutable version tag. For reproducible CI, pin third-party hook repositories to an immutable commit SHA so the same checkout cannot resolve different hook code if the tag is moved or re-created. |
| RGF-0007 | confirmed | .wt/setup | process-exec | The setup script executes a mutable remote installer directly with `curl \| sh`. Because `latest` can change and the script is not pinned or verified, anyone who compromises the release asset path or transport endpoint can execute arbitrary code during worktree setup. Prefer failing with an install instruction, or download a pinned artifact and verify a checksum before execution. |
| RGF-0008 | confirmed | .wt/setup | cli-contract | The setup script downloads and executes the latest installer from GitHub without pinning a version or verifying an expected checksum/signature. Worktree bootstrap scripts are commonly run automatically, so this makes setup non-reproducible and exposes users to upstream or network compromise. |
| RGF-0009 | confirmed | CONTRIBUTING.md | docs-accuracy | The documented project tree is out of date: it lists only api/auth/cli/commands/oauth/profile under src/, but the repository also contains top-level modules such as debug.rs plus idempotency/ and skills/. Contributors relying on this section will miss current source areas. |
| RGF-0010 | confirmed | CONTRIBUTING.md | docs-accuracy | The contributor coverage command uses `--timeout 120`, but the repository's CI coverage job runs tarpaulin with `--timeout 300 --out xml`. This makes the documented local check diverge from the maintained CI command and can cause false local timeouts or mismatched output expectations. |
| RGF-0011 | confirmed | Formula/slack_rs.rb | docs-accuracy | The Homebrew formula is stale: it points to tag v0.1.67 while the crate metadata in Cargo.toml declares version 0.1.71. Users installing from this formula will get an older release than the current project version. |
| RGF-0012 | confirmed | Makefile | data-validation | `publish-tag` accepts arbitrary tag text and passes it unquoted to `git checkout`, so a tag containing whitespace or shell metacharacters can be split or interpreted by the shell. Validate the tag format before use and quote the variable when checking it out. |
| RGF-0013 | confirmed | Makefile | ci-reproducibility | The local `make check` target is not reproducible with CI because it runs `cargo fmt` and plain `cargo clippy`, while the CI lint job runs `prek run --all-files` and `.pre-commit-config.yaml` enforces `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, YAML checks, and file hygiene. This lets `make check` pass locally while CI can still fail. |
| RGF-0014 | confirmed | README.md | docs-accuracy | The documented token storage path is inaccurate. The current FileTokenStore defaults to ~/.local/share/slack-rs/tokens.json and only migrates/uses ~/.config/slack-rs/tokens.json as the legacy path, so users following this section will inspect the wrong file on fresh installs. |
| RGF-0015 | confirmed | README.md | docs-accuracy | This wrapper command signature is missing the required --channel/--channels flag. The implemented usage is `file upload <path> [--channel=ID] [--channels=IDs] ...`, so the README example will pass the channel ID as the path and fail or upload the wrong file argument. |
| RGF-0016 | confirmed | skills/slack-rs/README.md | docs-accuracy | The recommended installation command points users to an external `tumf/skills` package, but this repository's current documented and implemented install path is `slack-rs install-skills` from the embedded `skills/slack-rs` assets. The only implemented installer sources are `self` and `local:<path>`, so this recommendation is stale and can lead users to install the wrong or unavailable skill source. |
| RGF-0017 | confirmed | skills/slack-rs/SKILL.md | docs-accuracy | The export example omits the required `--yes` flag. `handle_export_command` rejects `auth export` unless `--yes` is present, so the documented `slack-rs auth export --all --out all-profiles.enc --passphrase-prompt` command fails instead of exporting profiles. |
| RGF-0018 | dismissed | skills/slack-rs/references/recipes.md | docs-accuracy | The remote login recipe passes only --client-id with --cloudflared, but the implemented login flow requires both bot and user scopes unless they were already saved in profile config. As written, users following this SSH/tunnel recipe from a fresh profile will hit the missing --bot-scopes/--user-scopes validation rather than completing login. |
| RGF-0019 | confirmed | src/api/args.rs | docs-accuracy | Module documentation lists the supported flags as only `--json` and `--get`, but the parser also handles `--raw`, `--profile`, and `--token-type`. This makes the module-level usage summary stale for readers trying to understand the accepted arguments. |
| RGF-0020 | confirmed | src/api/args.rs | cli-contract | `--token-type` without a value is silently accepted and leaves `token_type` as `None`, so `slack-rs api call conversations.list --token-type` falls back to the profile default or inferred token instead of rejecting an incomplete value-taking option. This violates the CLI contract for an explicit `--token-type user\|bot` selector and can make users believe they forced a token type when they did not. |
| RGF-0021 | confirmed | skills/slack-rs/references/setup.md | docs-accuracy | This sentence implies that `config oauth set` supports `--bot-scopes` and `--user-scopes`, but that command only parses `--scopes`; passing either split-scope flag is rejected as an unknown option. Keep the split-scope guidance attached to `auth login`, or document only `--scopes` here. |
| RGF-0022 | confirmed | skills/slack-rs/references/setup.md | docs-accuracy | The storage locations are inaccurate: profile metadata is under `~/.config/slack-rs/profiles.json`, but the file token store defaults to `~/.local/share/slack-rs/tokens.json` (or `$XDG_DATA_HOME/slack-rs/tokens.json` / `SLACK_RS_TOKENS_PATH`), not under `~/.config/slack-rs/`. Users following this doc may back up or protect the wrong directory. |
| RGF-0023 | confirmed | src/api/client.rs | docs-accuracy | The doc comment says the backoff includes jitter, but the current arithmetic subtracts and adds the same value, leaving the backoff deterministic. This makes the documentation inaccurate and can mislead callers reviewing retry behavior. |
| RGF-0024 | confirmed | src/api/client.rs | cli-contract | The jitter calculation currently cancels itself out: the same `jitter / 2` value is subtracted and then added, so every retry sleeps for the deterministic exponential backoff duration. This defeats the intended jitter and can make concurrent CLI retries synchronize during Slack outages or rate limiting. |
| RGF-0025 | confirmed | src/api/guidance.rs | cli-contract | The guidance suggests running `slack auth login`, but this crate installs the CLI binary as `slack-rs` (Cargo.toml names the bin `slack-rs`). Users following this recovery instruction will hit a missing command unless they happen to have an unrelated `slack` binary installed, so the error guidance violates the CLI contract by emitting an invalid command. |
| RGF-0026 | confirmed | src/api/guidance.rs | cli-contract | The invalid_auth recovery text points users at `slack auth login`, but the actual binary provided by this project is `slack-rs`. This makes the CLI's error remediation command non-actionable for the common expired/revoked token path. |
| RGF-0027 | confirmed | src/api/guidance.rs | cli-contract | The token_revoked guidance emits `slack auth login`, which does not match the repository's CLI binary name (`slack-rs`). Error guidance should provide a command users can copy and run. |
| RGF-0028 | confirmed | src/api/guidance.rs | cli-contract | The token_expired remediation command uses `slack auth login` instead of the actual `slack-rs` executable, so users cannot reliably follow the printed recovery instructions. |
| RGF-0029 | confirmed | src/api/guidance.rs | cli-contract | The not_authed guidance tells users to run `slack auth login`, but the CLI binary is named `slack-rs`. This creates a broken first-time-auth recovery path. |
| RGF-0030 | confirmed | src/api/guidance.rs | cli-contract | The account_inactive guidance prints `slack auth login`, which is not the executable this crate installs. Use `slack-rs auth login` to keep the error guidance copy/pasteable. |
| RGF-0031 | confirmed | src/api/guidance.rs | docs-accuracy | The guidance text tells users to run `slack auth login`, but the documented and actual binary command in this repository is `slack-rs auth login`. This inaccurate remediation appears in user-facing error output and can send users to a non-existent or different CLI. |
| RGF-0032 | confirmed | src/api/guidance.rs | docs-accuracy | This resolution text also references `slack auth login`, which does not match the repository's CLI name (`slack-rs`). Error guidance should use the executable users install and the docs advertise. |
| RGF-0033 | confirmed | src/api/guidance.rs | docs-accuracy | This guidance points to `slack auth login`, but the CLI in this project is invoked as `slack-rs`. Keeping the command accurate matters because this string is printed directly to users when authentication is missing. |
| RGF-0034 | confirmed | src/api/mod.rs | docs-accuracy | Module documentation says this module provides wrapper commands for common operations, but this module only declares API-related submodules and re-exports types/functions. Wrapper commands live under `src/commands`, so this line can mislead readers about this module's responsibilities. |
| RGF-0035 | confirmed | src/auth/clipboard.rs | docs-accuracy | The documentation says the fallback chain tries OSC52 first, but copy_to_clipboard only attempts OSC52 when stdout is a TTY and SSH_CONNECTION or SSH_TTY is present. In non-SSH local terminals the first attempted method is arboard, so the module-level docs overstate the actual order. |
| RGF-0036 | confirmed | src/auth/cloudflared.rs | cli-contract | `Drop` writes lifecycle messages to stdout, which can pollute command output and break CLI consumers that parse stdout. Cleanup code should avoid normal stdout; use stderr/debug logging or stay silent. |
| RGF-0037 | confirmed | src/auth/cloudflared.rs | cli-contract | If URL extraction times out after spawning cloudflared, `start` returns an error without killing or waiting on the child process. Dropping `std::process::Child` does not terminate the process, so failed login attempts can leave orphaned tunnel processes running. |
| RGF-0038 | dismissed | src/auth/clipboard.rs | cli-contract | Linux clipboard helper discovery only tries to execute each helper with `--version` (and then the same binary with `-v`), so a helper that exists but does not support those flags or exits non-zero will be treated as unavailable even though the subsequent stdin-based invocation would work. That makes clipboard copy fail unnecessarily in otherwise valid CLI environments. Prefer PATH lookup that does not execute the helper with tool-specific flags, or just attempt `try_command_with_stdin` and rely on spawn failure for missing commands. |
| RGF-0039 | confirmed | src/auth/cloudflared.rs | docs-accuracy | The public API documentation for `start` says it starts a cloudflared tunnel and returns an instance, but it omits an important ownership side effect: the spawned tunnel is automatically killed when the returned `CloudflaredTunnel` is dropped. Callers reading the docs could accidentally drop the value immediately after reading `public_url()` and terminate the OAuth tunnel before the callback completes. Documenting this RAII behavior makes the lifecycle contract accurate. |
| RGF-0040 | confirmed | src/auth/commands.rs | docs-accuracy | The logout command documentation says it removes authentication, but the implementation only deletes the legacy bot token key and profile. User tokens saved under the separate `team_id:user_id:user` key and the saved OAuth client secret are left behind, so the documented behavior is misleading and stale credentials remain after logout. |
| RGF-0041 | confirmed | src/auth/commands.rs | cli-contract | `auth logout` only deletes the bot token key. Profiles created by the OAuth flow also store a user token at `team_id:user_id:user` and the OAuth client secret at `oauth-client-secret:<profile>`, so logout leaves usable credentials behind despite reporting that authentication was removed. |
| RGF-0042 | confirmed | src/auth/commands.rs | cli-contract | The extended login function documents cloudflared/ngrok tunnel support, but `use_cloudflared == false` falls back to a localhost redirect instead of starting an ngrok tunnel. Because the CLI accepts `--ngrok`, users can request ngrok and silently get a non-public callback URL, breaking the advertised command contract. |
| RGF-0043 | confirmed | src/auth/crypto.rs | cli-contract | derive_key ignores KdfParams.memory_cost, time_cost, and parallelism and always uses Argon2::default(), even though those parameters are serialized with exported files. Any export/import using non-default KDF metadata will not derive the documented key and the saved parameters become misleading. |
| RGF-0044 | confirmed | src/auth/crypto.rs | docs-accuracy | `KdfParams` exposes `memory_cost`, `time_cost`, and `parallelism`, and the comment says these are Argon2id KDF parameters, but `derive_key` ignores all three by constructing `Argon2::default()`. Exported payloads can therefore document non-default KDF parameters that are not actually used for key derivation, making the crypto metadata misleading and preventing parameter changes from taking effect. |
| RGF-0045 | confirmed | src/auth/export_import.rs | cli-contract | `fs::write` creates or truncates the export file before permissions are tightened to 0600, so on Unix a new backup can briefly exist with process-umask permissions (for example 0644) even though it contains encrypted Slack tokens. Create the file with mode 0600 from the start and write through that handle. |
| RGF-0046 | confirmed | src/auth/export_import.rs | cli-contract | When `--force` overwrites a conflicting profile, the old profile is removed from the config but its stored credentials are left behind. Because tokens are keyed by the old team/user pair and OAuth client secrets by profile name, forced imports can leave stale Slack credentials in the token store after the CLI reports an overwrite. |
| RGF-0047 | confirmed | src/auth/export_import.rs | docs-accuracy | Import drops persisted OAuth/profile metadata from the exported profile by hard-coding redirect_uri, scopes, and default_token_type to None. This conflicts with the profiles-and-token-store spec requiring OAuth non-sensitive information and default_token_type to persist, so imported profiles lose their OAuth callback/scopes and token selection behavior. |
| RGF-0048 | confirmed | src/auth/format.rs | cli-contract | KDF numeric fields are read as u64 and then cast to u32 with `as`, so a crafted export file with values larger than `u32::MAX` will silently truncate instead of being rejected. That can make malformed files decode to different parameters than were encoded and should be treated as invalid format data. |
| RGF-0049 | confirmed | src/auth/i18n.rs | docs-accuracy | The comment says the default is Japanese based on locale, but the implementation actually defaults to English unless LANG starts with `ja` (and also uses English when LANG is unset). This wording can mislead future maintainers about the fallback behavior. |
| RGF-0050 | confirmed | src/auth/manifest.rs | cli-contract | `generate_manifest` returns `Result<String, String>`, which loses typed error information and bypasses the project's `thiserror`-based error handling convention. This makes callers re-wrap opaque strings and weakens the CLI error contract. Use a typed error for manifest generation instead of raw strings. |
| RGF-0051 | confirmed | src/auth/manifest.rs | cli-contract | The generated YAML is rejected unless it starts directly with `_metadata:` or `"_metadata":`. YAML serializers may prepend a document marker (`---`) while still producing valid YAML, so this check can turn valid output into a CLI failure. Validate by parsing the YAML back or remove the serialization-order assertion. |
| RGF-0052 | confirmed | src/auth/manifest.rs | cli-contract | `profile_name` is embedded directly into the manifest display name, description, and bot display name without validation. A profile name containing newlines or characters unsupported by Slack bot display names can generate a manifest that the CLI writes/copies successfully but Slack rejects, breaking the manifest-first flow. Validate or sanitize profile names before building manifest fields. |
| RGF-0053 | confirmed | src/auth/manifest.rs | docs-accuracy | The test name and setup imply this covers a cloudflared-specific manifest, but the test passes a localhost redirect URI and only asserts generic scope/redirect behavior. This documentation-style test name is misleading and duplicates the non-tunnel behavior, so future readers may believe cloudflared redirect handling is verified here when it is not. |
| RGF-0054 | confirmed | src/auth/manifest.rs | docs-accuracy | This test has the same docs-accuracy issue as the cloudflared case: the name says ngrok, but the input is localhost and the assertions do not exercise an ngrok URL. That makes the test read as tunnel-specific documentation while only verifying generic redirect serialization. |
| RGF-0055 | confirmed | src/auth/ngrok.rs | docs-accuracy | The documentation states this extracts the public URL from ngrok output, but the implementation only accepts ngrok-free.app domains. Ngrok also emits other public hostnames depending on account/reserved domains, so the doc overstates supported ngrok output and can mislead callers when valid HTTPS tunnel URLs are ignored. |
| RGF-0056 | confirmed | src/auth/ngrok.rs | cli-contract | After sending the first public URL, both output-reader threads break and stop draining ngrok's stdout/stderr. If ngrok continues writing logs while the tunnel is active, the child process can block on a full pipe and the OAuth flow may hang. Keep reading after the URL is sent, as the cloudflared tunnel implementation does. |
| RGF-0057 | confirmed | src/auth/ngrok.rs | cli-contract | If URL extraction times out, start() returns an error while leaving the spawned ngrok process running. That leaks a background tunnel and can keep the local port/process alive after a failed login attempt. Kill and wait for the child before returning the timeout error. |
| RGF-0058 | confirmed | src/cli/handlers.rs | docs-accuracy | The guidance tells users to run `slackcli auth login (with user_scopes)`, but this crate's installed binary and help/introspection examples use `slack-rs`, and the actual flag is `--user-scopes`. As written, users following this remediation command will invoke the wrong command name and pass a non-existent option spelling. |
| RGF-0059 | confirmed | src/cli/help.rs | cli-contract | The import help text says `--yes` automatically accepts conflicts, but the implementation only uses `--yes` to confirm `--force`; conflicts are skipped unless `--force` is also provided. This can mislead users into expecting `--yes` alone to overwrite or resolve conflicts. |
| RGF-0060 | confirmed | src/cli/handlers.rs | cli-contract | The optional path parsed for --cloudflared/--ngrok is dropped before starting the tunnel. Passing --cloudflared /custom/bin/cloudflared still calls auth::login_with_credentials_extended with only a boolean, and that function rediscoveres cloudflared from PATH; --ngrok also falls through to the non-cloudflared branch and starts no ngrok tunnel at all. This violates the documented CLI contract for tunnel path selection and makes --ngrok ineffective. |
| RGF-0061 | confirmed | src/cli/help.rs | docs-accuracy | The import help says `--yes` automatically accepts conflicts, but the implementation only uses `--yes` to confirm overwrites when `--force` is also set. Without `--force`, conflicts are still skipped, so this help text can cause users to expect conflicts to be accepted when they are not. |
| RGF-0062 | confirmed | src/cli/introspection.rs | docs-accuracy | The structured help for `auth login` omits the supported `--cloudflared` and `--ngrok` tunnel options. `parse_login_args` accepts both flags, and the text help documents them, so agents relying on `commands --json` or `--help --json` will incorrectly conclude tunnel login is unavailable. |
| RGF-0063 | confirmed | src/cli/introspection.rs | docs-accuracy | The `conv list` introspection is missing several implemented flags (`--include-private`, `--all`, `--sort`, `--sort-dir`, and `--token-type`). Because this command definition is exposed as machine-readable CLI metadata, the omissions make the JSON help materially less accurate than the real parser. |
| RGF-0064 | confirmed | src/cli/introspection.rs | cli-contract | `commands --json` is advertised as the machine-readable list of available commands, but the definition table omits the implemented `doctor` command. Agents relying on introspection cannot discover `slack-rs doctor --json`, even though it is routed in `main.rs`. |
| RGF-0065 | confirmed | src/cli/introspection.rs | cli-contract | The `conv list` introspection omits supported CLI flags such as `--include-private`, `--all`, `--token-type`, `--sort`, and `--sort-dir`. This makes the machine-readable command contract incomplete for agents and scripts that rely on `commands --json` or `--help --json` rather than scraping human help. |
| RGF-0066 | confirmed | src/commands/config.rs | docs-accuracy | The test documentation asserts that oauth_show would print "(saved in file store)", but the implementation above prints "(saved in token store)". This makes the test comments and expected output documentation stale and misleading for anyone relying on tests as executable documentation. |
| RGF-0067 | confirmed | src/cli/mod.rs | docs-accuracy | The `conv select` usage text omits supported options. `run_conv_select` parses `--limit` and `--token-type`, so help output currently under-documents available flags. |
| RGF-0068 | confirmed | src/cli/mod.rs | docs-accuracy | The `conv history` usage text omits supported `--raw` output for the direct form and omits `--raw`/`--token-type` for the interactive form, even though `run_conv_history` parses both for all paths. |
| RGF-0069 | confirmed | src/commands/conv/api.rs | cli-contract | `conv_list` follows `next_cursor` until Slack stops returning one, but it has no cycle guard or maximum page bound. If Slack or a proxy repeatedly returns the same non-empty cursor, this CLI command can loop indefinitely and keep issuing API requests. Track seen cursors and fail on repeats to preserve the CLI contract of terminating predictably. |
| RGF-0070 | confirmed | src/commands/conv/api.rs | docs-accuracy | The documentation says conv_list returns an Ok(ApiResponse) with all pages aggregated, but the implementation uses ApiClient::call_method, which returns Err on any Slack response with ok=false. As a result, Slack API errors are not returned as Ok(ApiResponse) with ok=false/error populated, making the Returns documentation inaccurate for error responses. |
| RGF-0071 | confirmed | src/commands/conv/filter.rs | cli-contract | Wildcard name filters are documented as glob-style matching, but the wildcard branch is case-sensitive while non-wildcard matching is case-insensitive. This makes `--filter=name:test*` fail to match channels such as `TEST-channel`, even though `--filter=name:test` would match them. Normalize both pattern and text before wildcard matching, or document wildcard filters as case-sensitive. |
| RGF-0072 | confirmed | src/commands/conv/format.rs | cli-contract | Non-JSON formats silently turn Slack API errors into empty successful output. If conversations.list returns ok=false without a channels array, the jsonl/table/tsv branches return an empty string, so callers print nothing and still return Ok, which breaks the CLI contract for scripts because authentication/scope failures are indistinguishable from an empty result set. |
| RGF-0073 | dismissed | src/commands/conv/sort.rs | cli-contract | No issues found in this review cell. The sort key/direction parsing returns actionable errors with valid values, and sorting preserves the specified CLI contract for name, created, and num_members including missing numeric fields treated as 0. |
| RGF-0074 | confirmed | src/commands/conv/select.rs | cli-contract | Interactive prompts are written to stdout, so commands such as `conv select` and `conv search --select` do not emit a machine-readable channel ID only on stdout. This breaks CLI composition because the menu and prompt are mixed with the selected ID. Send human-facing prompts to stderr and reserve stdout for command results. |
| RGF-0075 | confirmed | src/commands/doctor.rs | cli-contract | User tokens are stored under the `team_id:user_id:user` key, but doctor checks `team_id:user_id_user`. As a result `slack-rs doctor` reports `userTokenExists: false` for profiles that actually have a user token, breaking the diagnostic contract and misleading users about missing scopes/tokens. |
| RGF-0076 | confirmed | src/commands/file.rs | cli-contract | When `file download --out -` is used, this function writes the raw file bytes to stdout and still returns metadata with `output: "-"`. The CLI wrapper suppresses its JSON envelope based on that metadata, but any diagnostic text emitted inside this function (or future wrapper changes) would corrupt the binary stdout stream. A safer CLI contract is for this command layer to avoid writing payload bytes directly to stdout; return the bytes to the caller so the wrapper can exclusively own stdout/stderr separation. |
| RGF-0077 | confirmed | src/commands/doctor.rs | docs-accuracy | `doctor` reports user token availability using a different key format from the OAuth credential save path. `save_profile_and_credentials` stores user tokens as `team_id:user_id:user`, while this code checks `team_id:user_id_user`, so `userTokenExists` can be false even when a user token is present. That makes the diagnostic output inaccurate for the documented token availability field. |
| RGF-0078 | confirmed | src/commands/guards.rs | cli-contract | Interactive confirmation uses unwrap() on stdout flush and stdin read_line, so an I/O error causes a panic instead of returning the function's declared ApiError result. This violates the CLI contract for fallible operations and can crash the command in closed-stdin or broken-pipe scenarios. |
| RGF-0079 | confirmed | src/commands/file.rs | docs-accuracy | The doc comment says `channels` accepts comma-separated channel IDs, but the implementation forwards the value unchanged as the singular `channel_id` parameter. This is misleading for callers because `--channels=C1,C2` will not be expanded into multiple shares; update the documentation to describe a single channel ID, or change the implementation to actually support comma-separated IDs. |
| RGF-0080 | confirmed | src/commands/mod.rs | docs-accuracy | Module documentation is out of date: it documents only file upload even though this module re-exports file_download, and it omits the public guards module entirely. This can mislead users browsing the Rust docs for available command modules. |
| RGF-0081 | dismissed | src/commands/react.rs | cli-contract | No issues found for this review cell. |
| RGF-0082 | confirmed | src/commands/msg.rs | cli-contract | The non-interactive retry hint interpolates the message text inside double quotes without shell escaping. If the text contains quotes, command substitutions, variables, or newlines, the example shown to automation/users is not a safe faithful rerun command, which weakens the CLI contract that the failure guidance provide a usable rerun example. |
| RGF-0083 | confirmed | src/commands/msg.rs | cli-contract | The update retry hint uses the placeholder "new text" instead of the actual requested text, so a non-interactive failure does not provide a faithful rerun example as required by the CLI contract. It also avoids the quoting problem only by omitting the real argument. |
| RGF-0084 | confirmed | src/commands/search.rs | cli-contract | This unit test verifies the function by making a real request with a fake token and asserting that it fails. That makes the test dependent on network behavior and Slack endpoint availability rather than the CLI wrapper contract. Prefer injecting or mocking the API client/base URL and asserting that search.messages receives the expected query/count/page/sort/sort_dir parameters. |
| RGF-0085 | confirmed | src/commands/thread.rs | docs-accuracy | The documentation says `inclusive` includes the parent message by default semantics, but this function only forwards Slack's `inclusive` parameter and does not set `oldest`/`latest`. The archived design notes that `--inclusive` has no effect without `oldest`/`latest`, so this doc overstates behavior and can mislead callers. |
| RGF-0086 | confirmed | src/commands/thread.rs | cli-contract | The pagination guard is intended to detect duplicate cursors, but the implementation only caps the number of pages. If Slack repeatedly returns the same non-empty next_cursor, this command will make up to 1000 duplicate API calls before failing, which is a poor CLI failure mode and contradicts the documented contract. Track seen cursors and fail immediately when a cursor repeats. |
| RGF-0087 | confirmed | src/commands/users.rs | cli-contract | This async unit test constructs the default API client and calls users.info against Slack's real API with a dummy token. That makes the default test suite depend on network availability and Slack response latency, so it can be flaky or slow while only asserting that some error occurred. Use a mocked/local API endpoint or refactor the parameter construction into a pure helper so the test stays deterministic. |
| RGF-0088 | confirmed | src/commands/users_cache.rs | cli-contract | Mention resolution only matches user IDs beginning with `U`, but Slack user mentions can also contain enterprise/user IDs beginning with `W`. Those IDs may be present in the users cache but will never be resolved, so `users resolve-mentions` silently leaves valid mentions unchanged. |
| RGF-0089 | confirmed | src/idempotency/store.rs | cli-contract | The store is persisted by truncating and rewriting the live JSON file in place. If the CLI is interrupted, the machine loses power, or the write fails after truncation, the idempotency store can be left empty or partially written; the next load then returns a JSON error and idempotency is disabled until the file is manually repaired. Write to a temporary file in the same directory and atomically rename it over the store file after a successful flush. |
| RGF-0090 | confirmed | src/idempotency/store.rs | docs-accuracy | The return documentation omits the expired-entry case. `check` calls `get`, and `get` filters out expired entries, so an existing but expired idempotency entry returns `Ok(None)` rather than an error or cached response. Documenting only 'entry doesn't exist' can mislead callers about replay behavior after TTL expiry. |
| RGF-0091 | confirmed | src/idempotency/types.rs | cli-contract | `ScopedKey` is serialized by concatenating raw components with `/`, but the components are user/API-controlled strings. If any component contains `/`, two different scoped keys can produce the same store key string, breaking the idempotency namespace and potentially replaying or rejecting the wrong request. Store a structured key or escape/encode each component before joining. |
| RGF-0092 | confirmed | src/main.rs | cli-contract | The top-level dispatch returns success for unknown commands after printing usage. For a CLI contract, an unrecognized command should fail with a non-zero status so scripts and agents can detect invalid invocations instead of treating them as successful no-ops. |
| RGF-0093 | dismissed | src/main.rs | docs-accuracy | The help text says file upload supports --idempotency-key, but the implementation path for file upload does not parse or apply an idempotency key. In src/cli/mod.rs, run_file_upload parses channel/title/comment/yes/profile/token type/raw only before calling commands::file_upload, so users following this help will get documentation for a safety feature that is not actually available for uploads. |
| RGF-0094 | dismissed | src/main.rs | docs-accuracy | The environment variable documentation states SLACKCLI_ALLOW_WRITE defaults to true. The actual guard defaults to allowing writes when the variable is unset, but write commands still require an explicit confirmation or --yes through confirm_destructive_with_hint. Saying the write control defaults to true can mislead users into thinking writes are fully enabled by default, while command help above says writes require SLACKCLI_ALLOW_WRITE=true. Reword this line to describe the unset behavior instead of a true default. |
| RGF-0095 | dismissed | src/main.rs | docs-accuracy | The short usage text also advertises --idempotency-key support for file upload, but the upload command implementation does not parse or use an idempotency key. Remove that claim here unless upload idempotency is implemented. |
| RGF-0096 | dismissed | src/oauth/pkce.rs | cli-contract | PKCE verifier and OAuth state are security-sensitive nonces, but rand::thread_rng() is not guaranteed to be a cryptographically secure RNG across rand versions/platforms. Use OsRng or another CSPRNG-backed source so verifier/state entropy does not depend on a general-purpose RNG implementation detail. |
| RGF-0097 | confirmed | src/oauth/port.rs | docs-accuracy | The doctest's default-port example is only correct when the caller's environment has no SLACK_OAUTH_PORT set, but the snippet does not clear it. Because doctests inherit the process environment, running docs/tests with SLACK_OAUTH_PORT already set will make the documented default assertion fail even though the implementation is behaving as documented. Make the example self-contained by clearing the variable before the default assertion and cleaning it up afterward. |
| RGF-0098 | confirmed | src/oauth/scopes.rs | cli-contract | `expand_bot_scopes` is the bot-context preset expander, but it expands `all`/`bot:all` through the older `bot_scopes()` helper instead of `bot_all_scopes()`. That produces a different scope set than `expand_scopes_with_context(..., true)` and the CLI help that says `--bot-scopes all` means the comprehensive bot preset; if this helper is used again, the same `all` input silently omits scopes such as `chat:write.public` and `channels:join` from the OAuth request. |
| RGF-0099 | confirmed | src/oauth/scopes.rs | cli-contract | `expand_user_scopes` has the same contract drift for the user-context `all` preset: it calls `user_scopes()`/`bot_scopes()` while the documented and currently wired context-aware expander uses `user_all_scopes()`/`bot_all_scopes()`. Reusing this public helper would make `all` omit user preset scopes such as `emoji:read`, `usergroups:read`, `usergroups:write`, and `users:read.email` from the OAuth URL. |
| RGF-0100 | confirmed | src/oauth/scopes.rs | docs-accuracy | The documentation says `bot_all_scopes()` excludes User-only scopes and lists `channels:write` as User only, but `bot_scopes()` still includes `channels:write` while its docs describe the list as bot-token scopes. This makes the older bot preset documentation inaccurate and can mislead callers of `expand_bot_scopes()` into requesting a user-only scope for bot OAuth. |
| RGF-0101 | confirmed | src/oauth/server.rs | docs-accuracy | The docs say callers should use port 3000, but the project now resolves the default OAuth callback port from `SLACK_OAUTH_PORT` with a fallback of 8765. Keeping 3000 here makes the public API documentation inconsistent with the actual login flow and can lead users to configure the wrong redirect URI. |
| RGF-0102 | confirmed | src/oauth/server.rs | cli-contract | The query parser splits each pair on every '=' and only keeps the first two pieces. Slack OAuth callback values are URL-encoded, but malformed or future parameters containing an unescaped '=' will be silently truncated. Use split_once so only the first separator divides key and value, matching query-string semantics. |
| RGF-0103 | confirmed | src/oauth/server.rs | cli-contract | The custom percent decoder pushes decoded bytes as individual chars, which corrupts non-ASCII UTF-8 sequences such as %E3%81%82 into mojibake. OAuth state/code are generally ASCII, but error messages and arbitrary query values can be UTF-8; percent decoding should collect bytes and validate UTF-8. |
| RGF-0104 | confirmed | src/profile/mod.rs | docs-accuracy | The module documentation says token storage uses the OS keyring, but the exported token store implementation currently creates a file-backed store via `FileTokenStore::new()`, which stores tokens in a JSON file with restricted permissions. This stale documentation can mislead users and reviewers about where Slack tokens are persisted and what security boundary protects them. |
| RGF-0105 | dismissed | src/profile/storage.rs | docs-accuracy | The migration helper documents the copy fallback as keeping the old file, but after a successful `fs::rename` the legacy file is removed. This makes the function behavior ambiguous for callers/tests reading the doc comment; clarify that the old file is kept only when the copy fallback is used. |
| RGF-0106 | confirmed | src/profile/token_store.rs | cli-contract | `set` mutates the in-memory token map before persisting it. If `save_tokens()` fails (for example due to a permission error, full disk, or the backing file being replaced by an unwritable path), the method returns an error but the current `FileTokenStore` instance now serves the unsaved token from memory. That can make later auth operations appear to have succeeded until the process restarts, while the credential was never durably stored. Persist a cloned updated map first, or roll back the insertion on save failure so the in-memory state matches the returned error. |
| RGF-0107 | confirmed | src/profile/token_store.rs | cli-contract | `delete` removes the token from memory before persisting the file. If `save_tokens()` fails, the method returns an error but this process has already forgotten the token, while the old token may still be present on disk. That creates inconsistent behavior between the running process and the next invocation. Restore the removed token when persistence fails, or write the updated contents atomically before committing the in-memory deletion. |
| RGF-0108 | confirmed | src/profile/token_store.rs | docs-accuracy | The FileTokenStore documentation says the default path is always `~/.local/share/slack-rs/tokens.json`, but `default_path()` first honors `SLACK_RS_TOKENS_PATH` and then absolute `XDG_DATA_HOME`. This can mislead callers about where credentials are stored by default on systems with XDG_DATA_HOME set. |
| RGF-0109 | confirmed | src/profile/token_type.rs | cli-contract | `FromStr` accepts values such as `" bot"` or `"user\n"` as invalid because it lowercases without trimming. For CLI input this makes otherwise normal whitespace-padded values fail, while the rest of the CLI commonly treats environment/config strings with trimming. Trim before matching, but keep the original value in the error for diagnostics. |
| RGF-0110 | confirmed | src/profile/types.rs | cli-contract | `list_names` returns `HashMap` keys in iteration order, so `slack auth list` can print profiles in a nondeterministic order across runs. For a CLI command, stable ordering avoids flaky snapshots/scripts and makes output predictable. |
| RGF-0111 | dismissed | src/skills/mod.rs | docs-accuracy | The doc comment says local installs go to ./.agents when global is false, but the implementation resolves this relative to std::env::current_dir(), not necessarily the package/project root. If the CLI is invoked from a subdirectory, the skill will be installed under that subdirectory's .agents, so the documentation overstates the behavior and can mislead callers. |
| RGF-0112 | confirmed | src/skills/mod.rs | cli-contract | Reinstalling the embedded skill over an existing local install can write through a symlink into the user's source skill directory. deploy_local_skill prefers creating .agents/skills/<name> as a symlink, but deploy_embedded_skill only calls create_dir_all and then fs::write on children. If a prior local:.../slack-rs install left .agents/skills/slack-rs as a symlink to an external directory, running the default self install will overwrite files in that external directory instead of replacing the install target under .agents/skills. Remove any existing target path with symlink-aware metadata before creating the embedded directory, matching the local install replacement behavior. |

## Triage Events

| Event | Finding | From | To | Reason |
| ---: | --- | --- | --- | --- |
| 1 | RGF-0001 | open | confirmed | 実ファイルで coverage.status.project と patch が off に設定されており、Codecov のカバレッジ status check が品質ゲートとして機能しないため実問題。 |
| 2 | RGF-0004 | open | confirmed | The workflow fetches the prek installer from the moving GitHub latest release URL and caches it under a latest key, so the installed binary can change without a workflow revision change. |
| 3 | RGF-0005 | open | confirmed | The workflow selects Rust with dtolnay/rust-toolchain@stable, so CI behavior can change as the stable channel advances without a workflow revision change. |
| 4 | RGF-0002 | open | confirmed | Release matrix uses mutable GitHub-hosted runner labels (ubuntu-latest, macos-latest, windows-latest) for tag builds, so the OS image can change over time for the same release workflow. |
| 5 | RGF-0003 | open | confirmed | Release workflow installs Rust via dtolnay/rust-toolchain@stable, so the compiler version follows the mutable stable channel rather than a pinned toolchain for reproducible tag builds. |
| 6 | RGF-0006 | open | confirmed | Remote third-party hook repo uses rev v4.6.0, a mutable tag rather than an immutable commit SHA; this is a real CI reproducibility issue. |
| 7 | RGF-0007 | open | confirmed | Real issue: .wt/setup executes a mutable latest remote installer via curl piped directly to sh, so the executed script is not pinned or verified. |
| 8 | RGF-0008 | open | confirmed | Real issue: .wt/setup downloads the latest GitHub installer without pinning a version or verifying an expected digest/signature. |
| 9 | RGF-0009 | open | confirmed | CONTRIBUTING.md lists src/{api,auth,cli,commands,oauth,profile} plus main/lib, but current src also contains debug.rs, idempotency/, and skills/. |
| 10 | RGF-0010 | open | confirmed | CONTRIBUTING.md documents cargo tarpaulin --timeout 120, while CI runs cargo tarpaulin with --timeout 300 --out xml in .github/workflows/ci.yml. |
| 11 | RGF-0011 | open | confirmed | Formula/slack_rs.rb points to v0.1.67 while Cargo.toml declares package version 0.1.71, so the Homebrew formula is stale. |
| 12 | RGF-0012 | open | confirmed | Makefile publish-tag reads arbitrary tag text and expands it unquoted in git checkout, allowing word splitting/globbing/option-like input to alter checkout arguments. |
| 13 | RGF-0013 | open | confirmed | Makefile check runs cargo fmt and plain cargo clippy, while CI runs prek with fmt --check, clippy --all-targets --all-features, and additional hooks, so local check is not CI-reproducible. |
| 14 | RGF-0014 | open | confirmed | README documents token storage as ~/.config/slack-rs/tokens.json, but FileTokenStore::default_path uses SLACK_RS_TOKENS_PATH, XDG_DATA_HOME/slack-rs/tokens.json, or ~/.local/share/slack-rs/tokens.json. |
| 15 | RGF-0015 | open | confirmed | README documents file upload <channel> <path>, but run_file_upload parses file upload <path> and optional --channel/--channels flags. |
| 16 | RGF-0016 | open | confirmed | skills/slack-rs/README.md recommends installing from external tumf/skills, but this repository documents and implements embedded skill installation via slack-rs install-skills in the project README and Source::SelfEmbedded; the README command is inaccurate for this repo. |
| 17 | RGF-0017 | open | confirmed | Confirmed: skills/slack-rs/SKILL.md export example omitted required --yes; handle_export_command exits without it. |
| 18 | RGF-0018 | open | dismissed | Dismissed: current tunnel login path uses login_with_credentials_extended, which defaults missing bot/user scopes via oauth::bot_all_scopes and oauth::user_all_scopes before calling auth; the non-interactive missing-scope validation applies only to the standard non-tunnel login path. |
| 19 | RGF-0021 | open | confirmed | Confirmed docs accuracy issue: skills/slack-rs/references/setup.md line 54 is in the config oauth set section and implies that config oauth set supports --bot-scopes/--user-scopes, but src/commands/config.rs OAuthSetParams only accepts a single --scopes value for that command; bot/user scope flags belong to auth login. |
| 20 | RGF-0022 | open | confirmed | Confirmed docs accuracy issue: skills/slack-rs/references/setup.md lines 82-85 state OAuth config and tokens are stored under ~/.config/slack-rs, but current storage code keeps profile metadata in ~/.config/slack-rs/profiles.json and file tokens/client secrets in ~/.local/share/slack-rs/tokens.json by default, with XDG_DATA_HOME or SLACK_RS_TOKENS_PATH overrides. |
| 21 | RGF-0020 | open | confirmed | ApiCallArgs::parse increments past --token-type and returns Ok with token_type None when no following value exists, silently accepting a missing required flag value. |
| 22 | RGF-0019 | open | confirmed | Module docs list only --json and --get, but ApiCallArgs::parse also handles --raw, --profile, and --token-type, so the documented supported flags are incomplete. |
| 23 | RGF-0023 | open | confirmed | Confirmed in src/api/client.rs:301-315: the documented jitter is not applied because the same jitter / 2 is subtracted and added, so calculate_backoff returns the capped exponential backoff unchanged. |
| 24 | RGF-0024 | open | confirmed | Confirmed in src/api/client.rs:310-315: the jitter arithmetic cancels itself out by subtracting and adding the same jitter / 2 value, so the CLI retry delay is deterministic instead of jittered. |
| 25 | RGF-0025 | open | confirmed | Confirmed: src/api/guidance.rs prints slack auth login, while Cargo.toml declares [[bin]] name = slack-rs, so the remediation command is not the installed CLI. |
| 26 | RGF-0026 | open | confirmed | Confirmed: invalid_auth guidance uses slack auth login, but Cargo.toml declares the executable as slack-rs. |
| 27 | RGF-0027 | open | confirmed | Confirmed: token_revoked guidance uses slack auth login, but the crate binary is slack-rs. |
| 28 | RGF-0028 | open | confirmed | Confirmed: token_expired guidance uses slack auth login, but the crate binary is slack-rs. |
| 29 | RGF-0029 | open | confirmed | Confirmed: not_authed guidance uses slack auth login, but the crate binary is slack-rs. |
| 30 | RGF-0030 | open | confirmed | Confirmed: account_inactive guidance uses slack auth login, but the crate binary is slack-rs. |
| 31 | RGF-0031 | open | confirmed | Confirmed: docs and Cargo.toml identify the CLI as slack-rs, but this guidance text says slack auth login. |
| 32 | RGF-0032 | open | confirmed | Confirmed: docs and Cargo.toml identify the CLI as slack-rs, but invalid_auth guidance says slack auth login. |
| 33 | RGF-0033 | open | confirmed | Confirmed: docs and Cargo.toml identify the CLI as slack-rs, but not_authed guidance says slack auth login. |
| 34 | RGF-0034 | open | confirmed | Module documentation lists wrapper commands as functionality of src/api/mod.rs, but the module only declares API-focused submodules and re-exports types/guidance; wrapper command implementations live outside this file. |
| 35 | RGF-0035 | open | confirmed | Module docs describe OSC52 as the first fallback entry, but copy_to_clipboard gates it behind should_try_osc52(), so arboard is the first attempted method outside SSH TTY contexts. |
| 36 | RGF-0038 | open | dismissed | command_exists uses Command::output().is_ok(), which ignores the helper exit status; an executable helper is treated as present even if --version or -v is unsupported and exits non-zero, so the claimed false-negative behavior does not occur. |
| 37 | RGF-0036 | open | confirmed | Confirmed: Drop writes lifecycle messages with println! on normal destruction, which can pollute stdout for CLI users parsing command output. |
| 38 | RGF-0037 | open | confirmed | Confirmed: start spawns cloudflared and, if recv_timeout fails before a URL is received, returns an error without killing or waiting on the child; dropping std::process::Child does not terminate the process. |
| 39 | RGF-0039 | open | confirmed | Confirmed: start documentation describes returning a tunnel instance but does not document the important lifecycle side effect that the instance owns a child process which is cleaned up on drop. |
| 40 | RGF-0040 | open | confirmed | Confirmed in src/auth/commands.rs:950-969: logout is documented as removing authentication, but it deletes only the bot-token key before removing the profile, leaving other OAuth credentials such as user-token/client-secret entries in token storage. |
| 41 | RGF-0041 | open | confirmed | Confirmed in src/auth/commands.rs:965-969 and src/auth/commands.rs:568-574: login stores user tokens at team_id:user_id:user, while logout deletes only make_token_key(team_id,user_id), so OAuth user-token credentials remain after auth logout. |
| 42 | RGF-0042 | open | confirmed | Confirmed in src/auth/commands.rs:1090-1095 and src/auth/commands.rs:1119-1172: the extended login docs claim cloudflared/ngrok tunnel support, but the function only handles cloudflared when requested and otherwise uses localhost; no ngrok branch is implemented. |
| 43 | RGF-0043 | open | confirmed | src/auth/crypto.rs:57-88 exposes KdfParams for Argon2id but derive_key constructs Argon2::default(), so memory_cost/time_cost/parallelism are ignored. |
| 44 | RGF-0044 | open | confirmed | src/auth/crypto.rs:30-46 documents and exposes KdfParams fields as KDF parameters, but derive_key does not apply those fields, making the API/docs contract inaccurate. |
| 45 | RGF-0045 | open | confirmed | Confirmed: write_secure_file uses fs::write before chmod, so a newly-created Unix export file can exist with umask-derived permissions before being tightened to 0600. |
| 46 | RGF-0046 | open | confirmed | Confirmed: force import removes the conflicting profile from config but does not delete token-store entries or profile-scoped OAuth client secret for the removed profile. |
| 47 | RGF-0047 | open | confirmed | Confirmed: import reconstructs Profile with redirect_uri, scopes, bot_scopes, user_scopes, and default_token_type set to None because ExportProfile does not carry those fields. |
| 48 | RGF-0048 | open | confirmed | KDF numeric fields are parsed as u64 and converted with as u32, so values above u32::MAX are silently truncated instead of rejected. |
| 49 | RGF-0049 | open | confirmed | Comment claims locale-based Japanese default, but implementation chooses Japanese only when LANG starts with ja and otherwise defaults to English. |
| 50 | RGF-0050 | open | confirmed | generate_manifest exposes Result<String, String> while auth/profile modules use typed thiserror errors, forcing callers such as generate_and_save_manifest to rewrap opaque strings. |
| 51 | RGF-0052 | open | confirmed | profile_name is CLI/user-provided and is inserted into Slack display fields without manifest-specific validation or sanitization, allowing generated YAML that can be written successfully but rejected by Slack. |
| 52 | RGF-0054 | open | confirmed | The test named ngrok passes a localhost callback and only asserts generic redirect/scope serialization, so it does not document or verify ngrok-specific behavior. |
| 53 | RGF-0051 | open | confirmed | The check validates YAML presentation and first-line ordering instead of manifest semantics, so a valid parseable document marker or serializer formatting change would be rejected. |
| 54 | RGF-0053 | open | confirmed | The test named cloudflared passes a localhost callback and only asserts generic redirect/scope serialization, so it does not document or verify cloudflared-specific behavior. |
| 55 | RGF-0055 | open | confirmed | Confirmed in src/auth/ngrok.rs:143-149: the public API/docs describe extracting a public ngrok URL generically, but the extractor only matches https://*.ngrok-free.app, excluding other valid ngrok public domains. |
| 56 | RGF-0056 | open | confirmed | Confirmed in src/auth/ngrok.rs:86-104: reader threads break after URL extraction and drop the pipe readers, so ngrok stdout/stderr are no longer drained while the tunnel is expected to keep running. |
| 57 | RGF-0057 | open | confirmed | Confirmed in src/auth/ngrok.rs:106-115: on recv_timeout error, start returns without killing or waiting for the spawned Child; dropping Child does not terminate the ngrok process. |
| 58 | RGF-0058 | open | confirmed | Confirmed: src/cli/handlers.rs:463 tells users to run slackcli auth login, but Cargo.toml installs the binary as slack-rs and other CLI help uses slack-rs; this guidance is inaccurate. |
| 59 | RGF-0060 | open | confirmed | Confirmed: parse_login_args preserves optional --cloudflared/--ngrok paths in TunnelMode, but run_auth_login only passes is_cloudflared() into login_with_credentials_extended; the selected path is dropped before tunnel startup. |
| 60 | RGF-0061 | open | confirmed | Confirmed: src/cli/help.rs:40 documents --yes as accepting conflicts, while import_profiles uses yes only to confirm --force overwrites; non-force conflicts are skipped. |
| 61 | RGF-0059 | open | confirmed | Confirmed: src/cli/help.rs:40 says --yes automatically accepts conflicts, but import_profiles only requires yes when force is set for overwrite confirmation and conflicts are skipped without --force. |
| 62 | RGF-0062 | open | confirmed | Confirmed: src/cli/handlers.rs parse_login_args accepts --cloudflared and --ngrok, and src/main.rs text help documents them, but src/cli/introspection.rs auth login flags list omits both. |
| 63 | RGF-0063 | open | confirmed | Confirmed: src/cli/mod.rs run_conv_list parses --include-private, --all, --sort, --sort-dir, and --token-type, while src/cli/introspection.rs conv list metadata omits them. |
| 64 | RGF-0064 | open | confirmed | Confirmed: src/main.rs routes the implemented doctor command with --json support, but src/cli/introspection.rs get_command_definitions does not include a doctor CommandDef. |
| 65 | RGF-0065 | open | confirmed | Confirmed: duplicate cli-contract issue for conv list metadata; implemented flags --include-private, --all, --token-type, --sort, and --sort-dir are omitted from the machine-readable command definition. |
| 66 | RGF-0067 | open | confirmed | run_conv_select parses --limit and --token-type, but conv select usage omits both supported options |
| 67 | RGF-0068 | open | confirmed | run_conv_history supports --raw in both modes and --token-type in interactive mode, but conv history usage omits those supported options |
| 68 | RGF-0066 | open | confirmed | Documented oauth_show output says '(saved in file store)', but implementation prints '(saved in token store)' in src/commands/config.rs. |
| 69 | RGF-0069 | open | confirmed | Confirmed: conv_list updates cursor from response_metadata.next_cursor and loops until None without tracking seen cursors or enforcing a page bound, so a repeated non-empty cursor can cause unbounded pagination requests. |
| 70 | RGF-0070 | open | confirmed | Confirmed: conv_list documentation says Ok(ApiResponse) for aggregated results, but client.call_method propagates Slack ok=false responses as ApiError, so the return docs understate Slack API error behavior. |
| 71 | RGF-0071 | open | confirmed | Confirmed: glob_match performs case-insensitive substring matching only when the pattern has no '*', but wildcard paths compare starts_with/ends_with/find against the original text and pattern. This makes name:test match TEST-channel while name:test* does not, despite the same user-facing name filter contract and wildcard glob behavior. |
| 72 | RGF-0072 | open | confirmed | Real issue: format_response handles jsonl/table/tsv by reading channels only and returns Ok(empty) when Slack API response has ok=false with no channels; run_conv_list/run_conv_search propagate format_response errors, so this should be an error instead of silent successful empty output. |
| 73 | RGF-0074 | open | confirmed | Interactive selection prompts in src/commands/conv/select.rs are emitted with println! to stdout at lines 65-69, which can contaminate command stdout intended for machine-readable CLI output. |
| 74 | RGF-0073 | open | dismissed | Finding content reports no issue; sort key and direction parsing in src/commands/conv/sort.rs returns explicit valid-value errors, so there is no cli-contract defect to fix. |
| 75 | RGF-0075 | open | confirmed | Confirmed: src/commands/doctor.rs builds the user token key as team_id:user_id_user, while the credential save path and callers use team_id:user_id:user for user tokens. |
| 76 | RGF-0077 | open | confirmed | Confirmed: doctor reports user token availability using team_id:user_id_user, which differs from the OAuth/profile credential save key team_id:user_id:user. |
| 77 | RGF-0079 | open | confirmed | src/commands/file.rs documents comma-separated channel IDs at line 40, but file_upload inserts the provided string unchanged as singular channel_id at lines 156-158. |
| 78 | RGF-0076 | open | confirmed | src/commands/file.rs writes downloaded bytes directly to stdout for --out - at lines 407-416, so command-layer stdout is mixed with payload output rather than owned solely by the CLI wrapper. |
| 79 | RGF-0078 | open | confirmed | Confirmed: src/commands/guards.rs interactive confirmation calls unwrap() on stdout flush and stdin read_line, so I/O errors panic instead of returning ApiError. |
| 80 | RGF-0080 | open | confirmed | Module docs mention file upload but omit the re-exported file_download command; documentation is out of date. |
| 81 | RGF-0082 | open | confirmed | The msg post non-interactive retry hint embeds user-provided message text inside double quotes without shell escaping, so quotes, command substitutions, dollars, or backticks can produce an unsafe or invalid copy-paste command. |
| 82 | RGF-0083 | open | confirmed | The msg update non-interactive retry hint uses the literal placeholder "new text" instead of the requested message text, so the retry command does not faithfully reproduce the failed operation. |
| 83 | RGF-0081 | open | dismissed | Finding content states no issues were found for this cli-contract review cell; metadata also records no actionable issue in src/commands/react.rs, so there is no defect to confirm. |
| 84 | RGF-0084 | open | confirmed | Test performs a live Slack API call with a fake token and only asserts failure, so it depends on network/external service behavior instead of a deterministic mock. |
| 85 | RGF-0085 | open | confirmed | src/commands/thread.rs only forwards Slack's inclusive parameter and does not set oldest/latest; archived design notes say inclusive has no effect without those bounds, so the doc comment claiming parent-message inclusion is misleading. |
| 86 | RGF-0086 | open | confirmed | src/commands/thread.rs documents duplicate cursor detection but maintains no seen-cursor set; a repeated non-empty next_cursor will loop until MAX_PAGES, so the documented CLI pagination guard is not implemented. |
| 87 | RGF-0087 | open | confirmed | users_info test constructs ApiClient::with_token, which uses the default Slack API base URL, so calling users_info sends a real users.info request with a dummy token. |
| 88 | RGF-0088 | open | confirmed | Mention regex only matches Slack user IDs beginning with U; Slack mentions may use other valid user ID prefixes, so those mentions remain unresolved. |
| 89 | RGF-0089 | open | confirmed | save writes the live store file directly with fs::write, so interruption can leave a truncated or partially written JSON file |
| 90 | RGF-0090 | open | confirmed | check returns Ok(None) for expired entries because get filters expired entries, but the return docs only mention missing entries |
| 91 | RGF-0091 | open | confirmed | ScopedKey Display uses raw slash-separated components as the persistent HashMap key, so user/API-controlled components containing '/' can produce ambiguous identical serialized keys across different scopes. |
| 92 | RGF-0094 | open | dismissed | Dismissed: check_write_allowed defaults to allow writes when SLACKCLI_ALLOW_WRITE is unset in src/commands/guards.rs:19-28, matching src/main.rs:603. |
| 93 | RGF-0092 | open | confirmed | Top-level unknown command falls through to print_usage without exiting non-zero in src/main.rs:198-200, so the CLI returns success for unrecognized commands. |
| 94 | RGF-0093 | open | dismissed | Dismissed: run_file_upload parses --idempotency-key via get_option at src/cli/mod.rs:1769 and applies idempotency handling at src/cli/mod.rs:1774-1866; the src/main.rs help text is accurate. |
| 95 | RGF-0095 | open | dismissed | Dismissed: run_file_upload parses --idempotency-key via get_option at src/cli/mod.rs:1769 and wraps idempotency metadata at src/cli/mod.rs:1858-1866, so short usage in src/main.rs:658 is accurate. |
| 96 | RGF-0096 | open | dismissed | Dismissed: rand 0.8 thread_rng returns ThreadRng, a cryptographically secure RNG reseeded from OS entropy and implementing CryptoRng; gen_range produces uniform indices, so this is not a real PKCE/state nonce weakness. |
| 97 | RGF-0097 | open | confirmed | Doctest default-port example depends on caller environment having no SLACK_OAUTH_PORT set; if set, the assertion expects 8765 but resolve_callback_port returns the environment port. |
| 98 | RGF-0098 | open | confirmed | expand_bot_scopes expands all/bot:all via legacy bot_scopes() instead of the documented comprehensive bot_all_scopes(), diverging from expand_scopes_with_context. |
| 99 | RGF-0099 | open | confirmed | expand_user_scopes expands all/user:all via legacy user_scopes() and bot:all via bot_scopes(), diverging from the comprehensive user_all_scopes()/bot_all_scopes() presets used by context-aware expansion. |
| 100 | RGF-0100 | open | confirmed | bot_all_scopes documentation identifies channels:write as user-only while the legacy bot_scopes preset still includes channels:write, creating inconsistent scope contract guidance in this file. |
| 101 | RGF-0101 | open | confirmed | Doc comment still says the callback port is typically 3000, but login resolves the runtime port via resolve_callback_port() with DEFAULT_OAUTH_PORT 8765 / SLACK_OAUTH_PORT override. |
| 102 | RGF-0102 | open | confirmed | parse_query_string splits query pairs with split('='), so values containing literal '=' are truncated instead of preserving everything after the first separator. |
| 103 | RGF-0103 | open | confirmed | The local percent decoder converts each decoded byte directly to char, which corrupts percent-encoded UTF-8 sequences instead of decoding byte sequences as UTF-8. |
| 104 | RGF-0104 | open | confirmed | src/profile/mod.rs documents token storage as OS keyring, but src/profile/token_store.rs exports create_token_store() as FileTokenStore and FileTokenStore stores tokens in a JSON file under the data directory. |
| 105 | RGF-0105 | open | dismissed | The helper documents the normal path as rename/move and only says the old file is kept when rename fails and the copy fallback runs; successful fs::rename removing the legacy file is consistent with that documentation. |
| 106 | RGF-0106 | open | confirmed | FileTokenStore::set updates the in-memory token map before save_tokens persists the file, so a persistence error leaves this store instance reporting the new token even though the on-disk store was not updated. |
| 107 | RGF-0107 | open | confirmed | FileTokenStore::delete removes the token from the in-memory map before save_tokens persists the deletion, so a persistence error leaves this store instance without the token even though the on-disk store still contains it. |
| 108 | RGF-0108 | open | confirmed | FileTokenStore::new uses default_path, which honors SLACK_RS_TOKENS_PATH and absolute XDG_DATA_HOME before falling back to ~/.local/share/slack-rs/tokens.json, so the documentation's always-default-path wording is inaccurate. |
| 109 | RGF-0109 | open | confirmed | Real CLI contract issue: TokenType::from_str lowercases input but does not trim surrounding whitespace, so values like ' bot' or 'user\n' are rejected despite being common CLI/config input normalization cases. |
| 110 | RGF-0110 | open | confirmed | ProfilesConfig stores profiles in a HashMap and list_names returns self.profiles.keys().cloned().collect(), so callers such as slack auth list receive HashMap iteration order rather than a deterministic order. |
| 111 | RGF-0111 | open | dismissed | The doc comment states local installs go to ./.agents when global=false, and this implementation resolves that as current_dir().join(".agents"). In CLI usage ./.agents is necessarily relative to the process current directory, so the comment is accurate for this file's behavior. |
| 112 | RGF-0112 | open | confirmed | deploy_embedded_skill writes embedded files directly into target_dir without removing or validating an existing target. If target_dir is an existing symlink to a source skill directory, fs::write(target_dir.join(rel_path), data) follows the symlink and overwrites the linked source files. |

## Blockers

None
